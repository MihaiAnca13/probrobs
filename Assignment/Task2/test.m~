%%
vel.Linear.X = 1;
vel.Angular.Z = 0;
last_pose = [0 0 0];
xs = []
ys = []
while(1)
    last_pose = vel_model(vel, last_pose);
    xs = [xs last_pose(
end

%%
figure(2);
plot(a(1), a(2), 'o');
axis([-5 5 -2.5 2.5]);